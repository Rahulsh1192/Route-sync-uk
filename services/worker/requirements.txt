# Core
redis==5.0.7
psycopg[binary]==3.2.1
boto3==1.34.140          # S3/R2 client
python-dotenv==1.0.1
httpx==0.27.0            # callback to API
pydantic==2.8.2

# Media / GPS
gpxpy==1.6.2
numpy==1.26.4
# Dashcam filenames carry local time with no zone marker, so Phase 24 resolves them
# through zoneinfo. Slim container images and Windows ship no system tz database, and
# without this the whole clock would silently fall back to UTC — an hour of drift
# through British Summer Time.
tzdata==2024.1
# ffmpeg/ffprobe are system binaries (invoked via subprocess), not pip packages.
# exiftool (optional) is used to read GPS embedded in dashcam video files.

# AI (heavy — install in the worker image, optional for local dev)
# opencv-python-headless==4.10.0.84
# ultralytics==8.2.0      # YOLO face/plate detection
# openai-whisper==20231117
